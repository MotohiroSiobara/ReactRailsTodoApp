import React, { Component, PropTypes } from 'react';

export default class AddListForm extends Component {
  render() {
    return (
        <form>
          <input type='text' />
        </form>
    )
  }
}
